import React from 'react';

const SearchInput = React.createClass({
    render () {
        return (
            <div>
                search input
            </div>
        )
    }
});

export default SearchInput;